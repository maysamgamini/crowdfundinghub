# Security

## Purpose
Bridges ASP.NET Core authentication data into the shared current-user abstraction consumed by application handlers.

## Files
- `HttpContextCurrentUser.cs`: Current-user adapter that reads identity and permission data from the HTTP context.

## Child Folders
- None.
