# Register User

## Purpose
Implements the Register User command workflow for the Identity module.

## Files
- `RegisterUserCommand.cs`: Command model dispatched through the application messaging pipeline.
- `RegisterUserCommandHandler.cs`: Application command handler for Register User.
- `RegisterUserCommandValidator.cs`: FluentValidation validator for Register User Command.
- `RegisterUserResult.cs`: Result model returned by a handler or query operation.

## Child Folders
- None.
