# Publish Campaign

## Purpose
Implements the Publish Campaign command workflow for the Campaigns module.

## Files
- `PublishCampaignCommand.cs`: Command model dispatched through the application messaging pipeline.
- `PublishCampaignCommandHandler.cs`: Application command handler for Publish Campaign.
- `PublishCampaignCommandValidator.cs`: FluentValidation validator for Publish Campaign Command.
- `PublishCampaignResult.cs`: Result model returned by a handler or query operation.

## Child Folders
- None.
