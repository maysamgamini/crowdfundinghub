# Reject Campaign Review

## Purpose
Implements the Reject Campaign Review command workflow for the Moderation module.

## Files
- `RejectCampaignReviewCommand.cs`: Command model dispatched through the application messaging pipeline.
- `RejectCampaignReviewCommandHandler.cs`: Application command handler for Reject Campaign Review.
- `RejectCampaignReviewCommandValidator.cs`: FluentValidation validator for Reject Campaign Review Command.
- `RejectCampaignReviewResult.cs`: Result model returned by a handler or query operation.

## Child Folders
- None.
