# Approve Campaign Review

## Purpose
Implements the Approve Campaign Review command workflow for the Moderation module.

## Files
- `ApproveCampaignReviewCommand.cs`: Command model dispatched through the application messaging pipeline.
- `ApproveCampaignReviewCommandHandler.cs`: Application command handler for Approve Campaign Review.
- `ApproveCampaignReviewCommandValidator.cs`: FluentValidation validator for Approve Campaign Review Command.
- `ApproveCampaignReviewResult.cs`: Result model returned by a handler or query operation.

## Child Folders
- None.
